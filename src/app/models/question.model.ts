/**
 * Create a simple interface
 * which reflects a question object
 * Usually developer gets it as a part of json
 */
export interface Question {
    id?: number;
    name?: string;
    value?: number;
}
