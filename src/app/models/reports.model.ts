/**
 *  Report Model interface defines all properties
 *  which this application processes
 */

 export interface Report {
     label?: string;
     data?: object;
     batch?: string;
     week?: string;
 }