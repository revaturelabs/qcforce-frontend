import { createSelector } from '@ngrx/store';
import { SurveysState } from '../states';

/*
export const getSurveyState = (state) => state.surveys;

export const getSurveysLoading = (state: SurveysState) => state.loading;
export const getSurveysLoaded = (state: SurveysState) => state.loaded;
export const getSurveys = (state: SurveysState) => state.data;

export const getAllSurveys = createSelector(getSurveyState, getSurveys);
export const getAllSurveysLoading = createSelector(getSurveyState, getSurveysLoading);
export const getAllSurveysLoaded = createSelector(getSurveyState, getSurveysLoaded);
*/
